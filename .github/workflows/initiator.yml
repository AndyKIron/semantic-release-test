name: CI-Started

on:
  push:
    branches-ignore: [ 'master' ]

  workflow_dispatch:

jobs:
  startCI:
    runs-on: ubuntu-latest
    steps:
      - name: Code Validate
        run: |
          echo "Validate Code"
          sleep 10s

      - uses: AndyKIron/trigger-action-workflow@master
        with:
          owner: ironsource-mobile
          repo: fusion-actions
          github_token: ${{ secrets.PAT_TOKEN }}
          workflow_file_name: code-validate.yml

      - name: Deploy Web App
        run: |
            echo "Deploy Web App"
            sleep 10s
