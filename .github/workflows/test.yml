name: TEST

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
#      - uses: actions/checkout@v2

      - name: Test
        run: echo "TEST-TSES"

      - name: Emit repository dispatch event
        run: curl \
          -X POST \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/octocat/hello-world/dispatches \
          -d '{"event_type":"event_type"}


#        run: curl -XPOST -u "${{ secrets.PAT_USERNAME}}:${{secrets.PAT_TOKEN}}" -H "Accept: application/vnd.github.v3+json" -H "Content-Type: application/json" https://api.github.com/repos/ironsource-mobile/fusion-actions/dispatches --data '{"event_type": "build_application"}'
